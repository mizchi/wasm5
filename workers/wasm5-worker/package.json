{
  "name": "wasm5-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd ../.. && moon build wasm5-api --target wasm-gc && cp _build/wasm-gc/release/build/wasm5-api/wasm5-api.wasm workers/wasm5-worker/src/",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
